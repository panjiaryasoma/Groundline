# Security tests

Must eventually include:
- source prompt injection remains untrusted data;
- agent direct accepted-state mutation rejected;
- agent direct acceptance rejected;
- malformed IDs/inputs return structured errors.
