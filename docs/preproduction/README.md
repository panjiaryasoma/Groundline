# Groundline active preproduction contracts

These files are copied from the frozen preproduction package so the implementation repository can be reviewed independently.

Authority order:
1. BASELINE_CONTRACT
2. FEATURE_SCHEMA_FINAL
3. accepted schema changes/finalization decisions
4. triage/integration acceptance artifacts
5. implementation

If code conflicts with the active contract, the code is wrong until a formal contract change is approved.
