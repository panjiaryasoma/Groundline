# P12.11 — Motion Pass

Visual/navigation-only polish.

- programmatic graph focus requests smoothly reveal the exact selected reasoning card
- START / INTAKE / DEMO / CUSTOM surfaces enter with a restrained fade-and-rise transition instead of blinking in
- intake step content gets a shorter matching transition
- selected graph cards receive a subtle arrival emphasis
- `prefers-reduced-motion` disables the motion layer
- no state-machine, WebMCP, authority, triage, or revision semantics change
